/** @type {import('tailwindcss').Config} */
module.exports = {
  // Сюди вказуєте, де шукати класи у вашому проєкті:
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      // Ваші кастомні кольори/шрифти тощо
    },
  },
  plugins: [
    // Тут підключені плагіни Tailwind (якщо потрібні)
  ],
};
